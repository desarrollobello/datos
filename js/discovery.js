/**
 * @file
 * Discovery behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.discovery = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
